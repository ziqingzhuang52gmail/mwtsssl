# 妖怪金手指 — 完整逆向工程手册

> 本手册涵盖全部逆向分析成果、代码定位、工具脚本、操作流程，确保新会话可无缝衔接。

---

## 一、目标系统信息

| 项目 | 值 |
|------|-----|
| 游戏名称 | 妖怪金手指 (大蓝微信专服) |
| SDK登录页 | http://sdk.ygkksy.com:9000/login |
| 游戏入口页 | http://sdk.ygkksy.com:2222/index.php?account=&token= |
| 游戏API | http://game.ygkksy.com:6162/sy_api/game_api.php |
| 游戏服WS | ws://game.ygkksy.com:31253 (3服-西瓜皮) |
| 核心JS文件 | assets/main/index.0f8ab.js (压缩单行, 约20MB+) |
| H5 SDK JS | h5SB_dalan.js (位于根目录) |
| 游戏引擎 | Cocos Creator v2.4.15 |
| 测试账号 | 13226410251 / Wonkef-ragci2-duqcuq |
| 伪造账号 | 18838681111 (任意手机号均可伪造登录) |

---

## 二、Sign签名逆向

### 2.1 算法

```
1. 参数按key字母升序排列
2. 拼接: key1=value1&key2=value2&...
3. 末尾拼接 secret_key
4. MD5哈希 → 转小写
```

### 2.2 密钥

```
secret_key = 507310f58a0ac8e3d8ad8f60c8b85b46td
```

- 来源: `AgentMgr._loginKey = "507310f58a0ac8e3d8ad8f60c8b85b46"` + 项目标识 "td"
- 位置: index.0f8ab.js 中 `calSign()` 方法内部硬编码

### 2.3 特殊规则

- `ext` 参数: sign计算时使用 **URL解码后的JSON明文**，非编码形式
- 空值参数 (`subPlatform=`): 正常参与排序和拼接
- `time` 参数: 毫秒级时间戳，但服务端 **不做过期校验** (1970年的时间戳都能通过)

### 2.4 已验证的3个接口

| action | 参数 | sign验证 |
|--------|------|---------|
| login | username, password, game_uin, token, pt_id, channelType, os_code, platform, subPlatform, ext, skipSdk | ✅ |
| pay_goods_list | os_code, platform, area, server_id, playerId, serVer, subPlatform, pt_id, time | ✅ |
| game_notice | game_notice, language, serVer, os_code, platform, subPlatform, pt_id, time | ✅ |

---

## 三、登录系统架构 (四层)

```
┌─────────────────────────────────────────────────────────────┐
│ 第一层: SDK网站登录 (需真实密码)                              │
│ POST http://sdk.ygkksy.com:9000/api/public/auth/member/login │
│ → 获取SDK token → 打开游戏入口页面                            │
│ → http://sdk.ygkksy.com:2222/index.php?account=&token=      │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│ 第二层: 游戏内PHP登录 (仅需account即可伪造)                    │
│ GET /sy_api/game_api.php?action=login                        │
│ → 获取 t_token + 服务器列表(svrListT)                        │
│ → skipSdk=1 绕过SDK验证                                     │
│ → token=local_token 伪造令牌                                 │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│ 第三层: LoginServer WebSocket (获取role信息)                   │
│ → loginR.role: {sessionkey, playerId, gameUrl, sGameUrl}    │
│ → 连接 ws:// + loginR.role.gameUrl                          │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│ 第四层: GameServer WebSocket (进入游戏)                       │
│ → LoginGame_LoginGame_C2S 协议包                             │
│   字段: name, sessionkey, deviceId, playerId,                │
│         username, codeVersion, connectState                  │
│ → 进入游戏主界面                                              │
└─────────────────────────────────────────────────────────────┘
```

### 3.1 IDOR漏洞验证

- **仅凭手机号即可伪造PHP登录**, 获取t_token
- `skipSdk=1` + `token=local_token` 绕过所有SDK认证
- 服务端完全信任客户端传入的 `username` / `game_uin`
- sign因密钥泄露可被任意伪造

### 3.2 渗透测试结果 (pay_goods_list)

| 参数 | 可篡改 | 影响 | 风险 |
|------|--------|------|------|
| playerId | ✅ | 越权查询任意玩家充值记录 | 高危 |
| area/server_id | ✅ | 跨区跨服查询 | 中危 |
| time | ✅ | 无过期校验，可重放 | 中危 |
| skipSdk注入 | ✅ | 接受额外参数 | 中危 |
| os_code/platform | ✅ | 不影响响应 | 低危 |

---

## 四、核心类与属性映射

### 4.1 Cocos Creator 类结构

| 类名 | 关键属性 | 作用 |
|------|---------|------|
| AgentMgr | _phpUuid, _sdkUuid, tryLoginAccount | 当前登录账号 |
| AgentMgr | _tokenFromPhp | PHP登录返回的t_token |
| AgentMgr | _selectServer / selectServer | 选区信息 |
| AgentMgr | loginR.role.{sessionkey, playerId, gameUrl} | LoginServer返回的角色数据 |
| AgentMgr | _loginKey | 签名密钥 |
| AgentMgr | _loginStatus | 登录状态(SocketStatusType枚举) |
| AgentConfig | _localAgent.AgentCode | 代理代码(如"wechat") |
| AgentConfig | _localAgent.skipSdk | "1"=跳过SDK |
| AgentConfig | _localAgent.PtId | "1" |
| AgentConfig | _localAgent.HttpSerever | game.ygkksy.com:6162/sy_api/game_api.php |
| AgentConfig | _localAgent.Project | "td" |
| AgentConfig | _checkConfig | 运行时配置字典(OpenGM, LogOpen等) |
| AgentConfig | _remoteAgent.checkConfig | 服务端下发的配置字符串 |
| SDKMgr | performConfig | 性能配置缓存(数字key) |
| SocketMgr | _address, _socket | WebSocket连接 |
| TestProxy | MakeTestOperRequest | GM命令发送方法 |
| UIMgr | open(view, layerType) | 打开UI面板 |
| OptionalModuleLoader | ensureGmBundleAndScripts() | 加载GM模块 |

### 4.2 关键枚举值

| 枚举 | 值 | 含义 |
|------|-----|------|
| PerformType.OpenGM | 20020 | GM开关 |
| PerformType.LogOpen | 1000 | 日志开关 |
| PerformType.DebugOpen | 30002 | 调试开关 |
| PerformType.AddResQuickly | (待查) | 快速添加资源 |
| UILayerType.GM | 11 (或10) | GM面板层级 |

### 4.3 __GM_BRIDGE__ 全局桥接对象

游戏加载后挂载在 `window.__GM_BRIDGE__`，包含：

- `AgentConfig`, `SDKMgr`, `UIMgr`, `GameMgr`, `AgentMgr` 等核心类引用
- `PerformType` 枚举 (含200+配置项，OpenGM=20020)
- `UILayerType` 枚举 (GM=11)
- `MakeTestOper_Test_C2S` GM命令C2S协议类
- `MakeTestOperR_Test_S2C` GM命令S2C响应类
- `TestProxy` GM命令代理
- `BattleProxy`, `HeroProxy`, `EquipProxy` 等业务代理

---

## 五、GM系统逆向

### 5.1 GM条件判断源码 (index.0f8ab.js 位置16953157)

```javascript
// 位于 Canvas 节点的组件 _tryOpenGMButton 方法中
_tryOpenGMButton: function() {
    // Step1: 检查是否应加载GM Bundle
    _shouldLoadGMBundle() {
        var e = "test" == AgentConfig.Ins.agentCode;  // agentCode需为"test"
        var t = true;  // 硬编码true，始终加载
        return t;
    }

    // Step2: 加载GM Bundle和脚本
    ensureGmBundleAndScripts() → 获取 gmModuleCtor, gmButtonCtor

    // Step3: checkOpen 检查 (❌ 硬编码false!)
    n = false;
    a = "test" == AgentConfig.Ins.agentCode;
    n = false;  // ← 关键: 无论agentCode是什么，n始终为false!

    // Step4: 条件判断
    if (n && this.gmButtonCtor) {
        UIMgr.Ins.open(gmButtonCtor, UILayerType.GM);  // 永远不会执行
    } else {
        console.warn("[Main][GM] skip open due to condition failed");  // 始终走这里
    }
}
```

**根因**: `n` 被硬编码为 `false`，即使 `agentCode="test"` 也无法通过。这是发布版本的安全措施。

### 5.2 绕过方案

**方案A**: 直接调用 `UIMgr.open(gmButtonCtor, UILayerType.GM)` 跳过条件检查

**方案B**: 修改 `getPerformConfig` Hook使OpenGM=1，然后重新触发

**方案C**: 用 `GMView` 类直接打开 (用户自有脚本的方式)

```javascript
const GMView = cc.js.getClassByName("GMView");
const UIMgr = cc.js.getClassByName("UIMgr");
UIMgr.Ins.open(GMView, 10);  // UILayerType.GM = 10 或 11
```

### 5.3 GM命令协议

```javascript
// 发送GM命令的代码格式
var e = new MakeTestOper_Test_C2S;
e.cmd = "AddRewards";                        // 命令名
e.extra = '{"rewards":"[[type, code, count]]"}';  // 参数JSON
TestProxy.Ins.MakeTestOperRequest(e, []);

// 协议层级: this._s.send(this._m, 174, e, t)
//   _m = Test模块号
//   174 = MakeTestOper 命令号
//   e = C2S数据
//   t = 回调
```

### 5.4 服务端校验与断线

**⚠️ 关键发现**: 执行GM命令后服务端会**主动关闭WebSocket连接**，导致断线踢出。

断线流程:
```
客户端发送 MakeTestOper_Test_C2S (Mod=Test, Cmd=174)
    ↓
服务端校验玩家是否为GM账号
    ↓ (非GM账号)
服务端主动关闭WebSocket
    ↓
客户端 onCloseHandler 触发
    ↓
SocketMgr 派发 SocketEventType.Close
    ↓
AgentMgr.socketConnectGameErrorHandler 触发
    ↓
输出 "==========socket断了========="
    ↓
_loginStatus = Error
    ↓
派发 AgentEventType.LoginGameError
    ↓
退回登录界面
```

**断线处理源码** (位置 6027443):
```javascript
socketConnectGameErrorHandler = function() {
    console.warn("==========socket断了=========");  // x5次
    var e = HttpMgr.isHttps() ? "wss://" : "ws://" +
            (HttpMgr.isHttps() ? this.loginR.role.sGameUrl : this.loginR.role.gameUrl);
    console.log("游戏服Socket链接错误", e);
    this._loginStatus = SocketStatusType.Error;
    this.dispatchEvent(AgentEventType.LoginGameError);
    LoginMgr.Ins.loginStageType = LoginStageType.CollectGameSocketErr;
}
```

### 5.5 绕过可能性分析

| 方案 | 可行性 | 说明 |
|------|--------|------|
| 拦截断线事件 | ❌ 不可行 | 服务端主动close，客户端无法阻止 |
| 伪造GM账号标识 | ❌ 不可行 | 服务端有独立的GM权限表，非客户端可控制 |
| 修改S2C响应 | ❌ 不可行 | 服务端不返回任何响应直接断开 |
| 中间人代理 | ⚠️ 理论可行 | 在WebSocket中间层拦截服务端close帧，但无法执行GM命令 |
| **仅打开GM面板不执行命令** | ✅ 可行 | GM面板UI可以正常显示，只是功能不可用 |
| **AddResQuickly功能** | ⚠️ 待验证 | 由 `getPerformConfig(PerformType.AddResQuickly, 0)` 控制，可能走不同路径 |

---

## 六、脚本文件清单

| 文件 | 用途 |
|------|------|
| `d:\Backup\Downloads\212\gm_force.js` | GM面板强制激活脚本 (含3步: 修改配置+Hook+打开面板) |
| `d:\Backup\Downloads\212\forge_login.js` | 任意账号伪造登录脚本 |
| `d:\Backup\Downloads\212\sign_verify.py` | Sign签名验证+渗透测试脚本 |
| `d:\Backup\Downloads\212\pay_test_lite.py` | pay_goods_list 接口渗透测试 |
| `d:\Backup\Downloads\212\jb.txt` | WebSocket协议日志Hook脚本 (v3.3) |
| `d:\Backup\Downloads\212\版本1.py` | pay_goods_list签名生成 (area=1) |
| `d:\Backup\Downloads\212\版本2.py` | pay_goods_list签名生成 (area=3) |
| `d:\Backup\Downloads\212\登录链接 (2).py` | login签名生成 |
| `d:\Backup\Downloads\212\核心逆向工程报告.md` | 之前的逆向报告 |
| `d:\Backup\Downloads\212\分析报告` | 渗透参数分析 |

---

## 七、操作流程速查

### 7.1 从零开始的环境搭建

```
1. 打开Chrome浏览器 (需要安装Chrome DevTools MCP)
2. 访问 http://sdk.ygkksy.com:9000/login
3. 输入账号 13226410251 密码 Wonkef-ragci2-duqcuq
4. 点击"进入游戏"按钮
5. 等待游戏Canvas加载完成 (约30秒)
6. 此时在Console中执行脚本
```

### 7.2 伪造任意账号登录

```javascript
// 在游戏页面Console中粘贴 forge_login.js 内容
// 修改顶部 TARGET_ACCOUNT 和 TARGET_SERVER
// 回车执行，5秒后自动验证
```

### 7.3 激活GM面板

```javascript
// 在游戏页面Console中粘贴 gm_force.js 内容
// 回车执行
// GM面板将自动打开
// ⚠️ 注意: 仅可查看面板，执行GM功能会导致断线
```

### 7.4 WebSocket协议监控

```
1. 在 index.0f8ab.js 第1行 第20568696列 设置断点
   (代码: this.log(" 收到 " + this.getCmdName(o, i) + " 来自 " + this.getModuleName(o)))
2. 在Console中执行 jb.txt 内容
3. 协议日志将输出到Console并存储在 window._aq_protocol_log
```

---

## 八、关键源码位置索引

以下为 index.0f8ab.js 中的字符位置（单行压缩代码，用 `text.indexOf()` 定位）:

| 功能 | 关键词/位置 | 说明 |
|------|------------|------|
| GM条件判断 | `skip open due to condition` (位置16953157) | `_tryOpenGMButton` 中n=false |
| GM Bundle加载 | `_shouldLoadGMBundle` | agentCode=="test"判断 |
| GM LOG_TAG | `[Main][GM]` (位置16955010) | `t.GM_LOG_TAG` |
| OptionalModuleLoader | `ensureGmBundleAndScripts` (位置18310450) | GM模块加载器 |
| MakeTestOperRequest | 位置22831805 | `this._s.send(this._m, 174, e, t)` |
| AddRewards示例 | 位置16366653 | GM添加资源命令示例 |
| calSign签名 | 搜索 `507310f58a0ac8e3d8ad8f60c8b85b46` | 签名密钥 |
| onCloseHandler | 位置12404534 | WebSocket关闭处理 |
| socketConnectGameErrorHandler | 位置6027443 | 游戏Socket断线处理 |
| loginGameServerSuccess | 位置6027900 | LoginGame_C2S协议构建 |
| getPerformConfig | SDKMgr原型方法 | PerformType枚举key→checkConfig→缓存 |

---

## 九、AgentConfig._localAgent 完整字段

```json
{
    "AgentCode": "wechat",          // 改为"test"可开启部分GM逻辑
    "skipSdk": "1",                 // 跳过SDK验证
    "DataBinPath": "config_all/weixin",
    "__KEY": "h5_ht",
    "__SDKGameUrl": "https://zeus.aidalan.com/v1/H5Game?game_id=191&channel_id=282...",
    "__serverKEY": "wechat",
    "clientType": "1",
    "h5SBFileName": "h5SB_dalan.js",
    "indexHtmlFile": "indexRelease.html",
    "useThirdSystem": "1",
    "HttpSerever": "game.ygkksy.com:6162/sy_api/game_api.php",
    "Project": "td",               // 项目标识(附加在密钥后)
    "PtId": "1",
    "ServerName": "大蓝微信专服",
    "DistTime": "20260615 23:00:50",
    "ClientVer": "129091",
    "ConfigVer": "128977",
    "BuildTime": "20260615 16:57:53"
}
```

### AgentConfig._remoteAgent 完整字段

```json
{
    "http_server": "http://game.ygkksy.com:6162/sy_api/game_api.php",
    "http_server_shenhe": "http://game.ygkksy.com:6162/sy_api/game_api.php",
    "res_host": "",
    "checkConfig": "[\"LogOpen:0\",\"DebugOpen:0\",\"_OpenGM:0\",\"CloseAutoLogin:1\",\"isAdvertise:1\"]",
    "cver": "999999",
    "JSVer": "",
    "configVer": "weixin84847",
    "version": "online"
}
```

---

## 十、PerformType 完整枚举 (GM相关)

| key | 数字值 | 含义 |
|-----|--------|------|
| LogOpen | 1000 | 日志开关 |
| OpenGM | 20020 | GM开关 (⚠️ 配置键名为OpenGM, 非_OpenGM) |
| DebugOpen | 30002 | 调试开关 |
| AddResQuickly | 待查 | 快速添加资源(GM面板的添加资源功能) |
| AlphaMode | 待查 | Alpha测试模式 |
| BattleLabFunction | 待查 | 战斗实验室功能 |
| OpacityGM | 待查 | 透明GM |
| ValidUserNames | 待查 | 有效用户名列表(GM白名单?) |

> `getPerformConfig` 逻辑: 先查 `performConfig[数字key]` 缓存 → 未命中则查 `AgentConfig.Ins.checkConfig[PerformType[数字key]]` → 赋值缓存并返回

---

## 十一、已知限制与未完成项

1. **GM功能断线问题**: 服务端校验GM权限，非GM账号执行命令会被踢出，目前无绕过方案
2. **AddResQuickly**: 由 `getPerformConfig(PerformType.AddResQuickly, 0)` 控制，Hook返回1后GM面板中的添加资源按钮可见，但执行仍走MakeTestOper协议，同样会被踢
3. **ValidUserNames**: PerformType中有此配置项，可能是GM白名单机制，待深入分析
4. **服务端GM校验**: 需要抓包确认服务端收到MakeTestOper后的具体响应(是返回错误码还是直接断开)
5. **BuildSkin(Mod 91)**: 之前发现的SSNV漏洞(服务端无验证)仍然有效，可用于免费激活建筑皮肤
6. **中间人WS代理**: 理论上可在WebSocket层面拦截服务端close帧保持连接，但GM命令仍无法生效

---

## 十二、新会话衔接指南

在新Chat中粘贴以下内容即可无缝衔接:

```
我正在逆向分析游戏"妖怪金手指"，请先阅读以下文件获取完整上下文：
- d:\Backup\Downloads\212\逆向工程完整手册.md (本文件)
- d:\Backup\Downloads\212\gm_force.js (GM激活脚本)
- d:\Backup\Downloads\212\forge_login.js (伪造登录脚本)

核心信息:
- 游戏API: http://game.ygkksy.com:6162/sy_api/game_api.php
- secret_key: 507310f58a0ac8e3d8ad8f60c8b85b46td
- 测试账号: 13226410251 / Wonkef-ragci2-duqcuq
- Cocos Creator v2.4.15 引擎
- GM面板可强制打开但执行命令会被服务端踢出
- 当前需要解决: [描述你的新需求]
```
