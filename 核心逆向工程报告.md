# 游戏API签名逆向与账号伪造登录 - 核心逆向工程报告

## 一、目标系统概述

| 项目 | 信息 |
|------|------|
| 游戏名称 | 妖怪金手指 |
| SDK入口 | http://sdk.ygkksy.com:9000/ |
| 游戏入口 | http://sdk.ygkksy.com:2222/index.php |
| 游戏API | http://game.ygkksy.com:6162/sy_api/game_api.php |
| 核心JS | assets/main/index.0f8ab.js |

## 二、Sign签名逆向

### 2.1 Sign生成算法

```
1. 参数按key字母升序排列
2. 拼接: key1=value1&key2=value2&...
3. 末尾拼接secret_key
4. MD5哈希 → 转小写
```

### 2.2 泄露的密钥

```
secret_key = 507310f58a0ac8e3d8ad8f60c8b85b46td
```

来源: AgentMgr._loginKey = `507310f58a0ac8e3d8ad8f60c8b85b46`（去掉项目标识"td"后即为基础密钥）

### 2.3 特殊规则

- `ext`参数: sign计算时使用**URL解码后的JSON明文**，非编码形式
- 空值参数(`subPlatform=`): 正常参与排序和拼接

### 2.4 验证结果

| 请求 | 原始sign | 计算sign | 匹配 |
|------|---------|---------|------|
| login | e32f0027143d8cdc7b9ac6defd94aabe | e32f0027143d8cdc7b9ac6defd94aabe | ✅ |
| pay_goods_list | 3dedda0d53f51e5cd53c8c52d43b1f48 | 3dedda0d53f51e5cd53c8c52d43b1f48 | ✅ |
| game_notice | ed6d0fe2775ff90004386395e145eb3f | ed6d0fe2775ff90004386395e145eb3f | ✅ |

## 三、登录系统架构逆向

### 3.1 两层登录体系

```
┌─────────────────────────────────────────────────────┐
│  第一层: SDK网站登录 (需真实密码)                      │
│  POST /api/public/auth/member/login                  │
│  → 获取SDK token → 打开游戏入口页面                    │
│  → http://sdk.ygkksy.com:2222/index.php?account=&token= │
└──────────────────────┬──────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────┐
│  第二层: 游戏内PHP登录 (仅需account即可伪造)            │
│  GET /sy_api/game_api.php?action=login               │
│  → 获取t_token + 服务器列表                           │
│  → skipSdk=1 绕过SDK验证                             │
│  → token=local_token 伪造令牌                         │
└──────────────────────┬──────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────┐
│  第三层: LoginServer WebSocket (获取role信息)          │
│  → loginR.role: {sessionkey, playerId, gameUrl}      │
└──────────────────────┬──────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────┐
│  第四层: GameServer WebSocket (进入游戏)               │
│  → 用loginR.role信息发送LoginGame_C2S协议包           │
│  → 进入游戏主界面                                     │
└─────────────────────────────────────────────────────┘
```

### 3.2 核心类与属性 (Cocos Creator引擎)

| 类名 | 关键属性 | 作用 |
|------|---------|------|
| AgentMgr | _phpUuid, _sdkUuid, tryLoginAccount | 当前登录账号 |
| AgentMgr | _tokenFromPhp | PHP登录返回的t_token |
| AgentMgr | _selectServer / selectServer | 选区信息 |
| AgentMgr | loginR.role.{sessionkey, playerId, gameUrl} | LoginServer返回的角色数据 |
| AgentMgr | _loginKey | 签名密钥(507310f58a0ac8e3d8ad8f60c8b85b46) |
| AgentConfig | skipSdk=1, PtId="1", clientPasswd="" | 配置项 |
| SocketMgr | _address, _socket | WebSocket连接 |

### 3.3 关键方法调用链

```
doLogin(account)
  → 构建login参数 + calSign() → HTTP GET game_api.php?action=login
  → loginHandler() → 设置 _phpUuid, _tokenFromPhp, selectServer
  → 触发 AgentEventType.LoginPhpSuccess

connectGameServer()
  → SocketMgr.init(ws:// + loginR.role.gameUrl)
  → SocketMgr.connect()
  → socketConnectGameHandler() → loginGameServerSuccess()
  → LoginGameProxy.LoginGameRequest(LoginGame_C2S)
  → loginGameSuccessHandler() → 进入游戏
```

## 四、IDOR漏洞验证

### 4.1 漏洞原理

1. `secret_key`硬编码在客户端JS中，任何人可提取
2. `skipSdk=1`参数可跳过第三方SDK认证
3. `token=local_token`可伪造任意令牌
4. 游戏API完全信任客户端传入的`username/game_uin`
5. sign因密钥泄露可被任意伪造

### 4.2 实测验证

| 测试项 | 结果 |
|--------|------|
| 仅凭手机号18838681111伪造PHP登录 | ✅ 成功，获取t_token |
| 修改AgentMgr状态为18838681111 | ✅ 成功 |
| 构造loginR跳过LoginServer | ✅ 成功 |
| connectGameServer连接3服 | ✅ WebSocket OPEN |
| 以18838681111身份进入游戏 | ✅ 成功 |

### 4.3 可渗透接口清单

| 接口 | action | 可篡改参数 | 风险等级 |
|------|--------|-----------|---------|
| 登录 | login | username, password, game_uin, skipSdk | 🟢高危 |
| 支付商品 | pay_goods_list | playerId, area, server_id | 🟢高危 |
| 公告 | game_notice | - | ⚪低危 |
| 服务器列表 | get_AllServerList | username | 🟡中危 |
| 版本信息 | get_versionInfoApi | - | ⚪低危 |

## 五、伪造登录脚本使用方法

### 5.1 前置条件

1. 浏览器已打开游戏页面（任意账号登录SDK后进入游戏即可）
2. 游戏页面已完全加载（Canvas已渲染）

### 5.2 操作步骤

1. F12打开Console
2. 粘贴 `forge_login.js` 全部内容
3. 修改顶部 `TARGET_ACCOUNT` 为目标账号
4. 按需修改 `TARGET_SERVER` 为目标服务器
5. 回车执行
6. 等待5秒后查看结果

### 5.3 快捷修改账号

只需修改脚本顶部的2个变量：

```javascript
const TARGET_ACCOUNT = "目标手机号";
const TARGET_SERVER = {
    s_id: "服务器ID",      // 如 "3"
    s_name: "服务器名称",   // 如 "3服-西瓜皮"
    area: "区号",          // 如 "3"
    port: "端口"           // 如 "31253"
};
```

### 5.4 验证命令

```javascript
verifyLogin()  // 查看当前登录状态
```

## 六、漏洞根因分析

| 漏洞 | 根因 | 修复建议 |
|------|------|---------|
| secret_key泄露 | 硬编码在前端JS | 迁移签名逻辑到服务端 |
| skipSdk绕过 | 客户端可控 | 服务端强制校验SDK |
| IDOR任意登录 | 服务端信任客户端uid | 服务端校验session/cookie |
| 密码明文传输 | HTTP无加密 | 强制HTTPS + 密码加密 |
| WebSocket无认证 | WS连接无鉴权 | 增加WebSocket握手token验证 |
