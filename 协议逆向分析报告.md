# WebSocket 自定义协议逆向分析报告

> 项目：金手指（修仙游戏）
> 脚本版本：v4.2
> 报告日期：2026-08-01

---

## 一、项目概述

### 1.1 目标

对基于 Cocos Creator 引擎的 HTML5 游戏"金手指"进行 WebSocket 自定义协议逆向分析，实现：

- 捕获客户端与服务器之间的所有 WebSocket 通信
- 将 Protobuf 二进制协议解码为可读的 JSON 明文
- 以单行格式输出到浏览器控制台，便于调试分析
- 将协议记录持久化到全局数组，供后续分析

### 1.2 技术栈

| 组件 | 技术 |
|------|------|
| 游戏引擎 | Cocos Creator (cc) |
| 脚本语言 | JavaScript (ES5/ES6) |
| 通信协议 | WebSocket + 自定义二进制协议 |
| 序列化格式 | Protobuf (protobuf.js) |
| 压缩算法 | QuickLZ |
| 调试工具 | Chrome DevTools MCP |

---

## 二、逆向分析过程

### 2.1 环境侦察

**入口文件分析：**

游戏主页面加载 `h5SB_dalan.js` 作为启动器，随后加载核心脚本 `index.0f8ab.js`（约 24MB），其中包含游戏的所有业务逻辑、网络通信层和 Protobuf 定义。

**关键全局对象诊断：**

```javascript
// 检查核心模块是否存在
typeof SocketMgr       // → "function"
typeof GameRoomSocket  // → "function"
typeof cc              // → "object"
typeof cc.js.getClassByName  // → "function"
```

**WebSocket 类定位：**

通过 Cocos Creator 的类系统 `cc.js.getClassByName()` 定位到两个核心通信类：

- `SocketMgr` — 主游戏 Socket 管理器，处理大部分协议
- `GameRoomSocket` — 副本/房间场景 Socket 管理器

### 2.2 协议数据流分析

通过断点调试和源码分析，还原出完整的数据处理链路：

```
服务器原始二进制
       ↓
WebSocket.onmessage (原始帧)
       ↓
SocketMgr.onMessageHandler (入口)
       ↓
SocketMgr.onMessageParser (解压缩 + 协议头解析)
       ↓
SocketMgr.parserS2C (Protobuf 解码)
       ↓
SocketMgr.log (日志输出 + 业务回调)
```

**关键断点位置：** `index.0f8ab.js` 第 1 行，第 20568696 列

```javascript
// 断点处代码
this.log(" 收到 " + this.getCmdName(o, i) + " 来自 " + this.getModuleName(o) + ", 错误码:")
```

### 2.3 协议帧结构

#### S2C (Server → Client) 帧结构

| 字段 | 偏移 | 长度 | 说明 |
|------|------|------|------|
| Size | 0 | 2+ | 数据包总长度（变长编码） |
| SSID | 2+ | 1+ | 会话ID（变长编码） |
| Module | 3+ | 1+ | 模块ID（变长编码） |
| Cmd | 4+ | 1+ | 命令ID（变长编码） |
| IsCompress | 5+ | 1 | 压缩标志 (0/1) |
| Body | 6+ | 变长 | 协议体（可能经 QuickLZ 压缩） |

#### C2S (Client → Server) 帧结构

```javascript
// send(module, cmd, data)
// module: 模块对象
// cmd: 命令ID
// data: Protobuf 编码后的数据对象
```

### 2.4 模块/命令系统

游戏采用模块-命令的二级路由体系：

- **模块 (Module)**：按业务领域划分，如 `ChatProxy(19)`、`ActivityProxy(18)`、`LoginGameProxy(2)`、`TaskProxy(17)`
- **命令 (Cmd)**：模块内的具体操作，如 `NewMessageP(7)`、`Ping(8)`、`ActivityListP(18)`、`TaskP(7)`

模块和命令通过 `getModuleName()` 和 `getCmdName()` 方法映射为人类可读的名称。

---

## 三、脚本原理与技术实现

### 3.1 核心架构：猴子补丁 (Monkey Patch)

脚本通过替换 `SocketMgr.prototype` 和 `GameRoomSocket.prototype` 上的三个关键方法实现协议拦截：

```
┌─────────────────────────────────────────────────┐
│                 钩子架构图                        │
│                                                   │
│  send (C2S) ───→ 拦截发送数据，输出JSON明文       │
│                   保留原始功能 (os.apply)          │
│                                                   │
│  parserS2C ───→ 拦截解码结果，输出JSON明文        │
│                   保留原始功能 (op.apply)          │
│                   捕获原始函数返回值               │
│                                                   │
│  log ────────→ 静默捕获已解码数据                  │
│                   不输出任何内容                   │
│                   记录到 _aq_protocol_log          │
└─────────────────────────────────────────────────┘
```

### 3.2 三个钩子的职责

#### 钩子 1：`send` — C2S 发送拦截

```javascript
p.send = function(m, cd, d) {
  // 1. 解析模块名和命令名
  var mn = this.getModuleName ? this.getModuleName(m) : '?';
  var cn2 = this.getCmdName ? this.getCmdName(m, cd) : '?';
  
  // 2. 序列化数据对象为JSON
  var dataStr = d !== undefined ? objToStr(d) : '{}';
  
  // 3. 单行输出
  console.warn('🚀 [C2S]' + cn + '.' + mn + '.' + cn2 + ' ' + dataStr);
  
  // 4. 调用原始函数
  return os.apply(this, arguments);
};
```

#### 钩子 2：`parserS2C` — S2C 接收拦截

```javascript
p.parserS2C = function(sz, ss, md, cm, ic, b) {
  // 1. 设置上下文，供 log 钩子填充
  window._aq_s2c_current = {...};
  
  // 2. 调用原始解析函数
  var result = op.apply(this, arguments);
  
  // 3. 优先使用 log 钩子捕获的已解码数据
  var finalData = window._aq_s2c_current.logData || result;
  
  // 4. 序列化并输出
  var dataStr = objToStr(finalData);
  if (valid) console.warn('📥 [S2C]' + cn + '.' + mn + '.' + cn2 + ' ' + dataStr);
  
  return result;
};
```

#### 钩子 3：`log` — 静默数据捕获

```javascript
p.log = function() {
  // 1. 捕获 log 传入的已解码数据（更可靠）
  if (arguments.length >= 2) {
    si.logData = arguments[1];
  }
  
  // 2. 记录到协议日志数组
  window._aq_protocol_log.push({...});
  
  // 3. 不输出任何内容
};
```

### 3.3 对象序列化引擎 `objToStr()`

`objToStr()` 是脚本的核心工具函数，负责将 Protobuf 解码后的对象转换为可读的 JSON 字符串。

**序列化策略（三层回退）：**

```
第一层：JSON.stringify(plainObject)
  ├── 逐字段复制，过滤 function/undefined
  ├── 特殊类型转换：Binary→[Binary:N B]、Long→string
  ├── 循环引用检测：WeakSet
  ├── 深度限制：max depth = 5
  └── 长度限制：max 8000 chars

第二层：手动逐字段拼接 {key=value, ...}
  └── 当 JSON.stringify 失败时回退

第三层：String(obj)
  └── 最终保底方案
```

**支持的特殊类型：**

| 类型 | 处理方式 |
|------|---------|
| Uint8Array | `[Uint8Array:12B]` |
| ArrayBuffer | `[ArrayBuffer:1024B]` |
| Long/Int64/UInt64 | 调用 `toString()` 转为字符串 |
| 循环引用 | `[Circular]` |
| Function | `[fn functionName]` |
| 大数组 (>30) | 截断并显示 `...(N more)` |

---

## 四、迭代历程与问题修复

### 4.1 版本演进

| 版本 | 核心改进 |
|------|---------|
| v1.4 | 初始版本，Hook SocketMgr 的 send/log 方法 |
| v2.0 | 增加 GameRoomSocket 支持，修复闭包变量访问 |
| v3.1 | 修复模块名重复显示，添加 objToStr 序列化函数 |
| v3.3-full | 修复 `[instanceof e]` 显示问题，log 钩子不再调用原始函数，支持压缩数据解码 |
| v4.0 | 单行整合输出，去除 Hex 显示，log 钩子静默化 |
| v4.2 | 添加 `_aq_orig` 持久化引用机制，防止重复注入链式调用，恢复原始函数逻辑 |

### 4.2 关键问题修复记录

#### 问题 1：控制台输出 `[instanceof e]` 而非 JSON

**根因：** `log` 钩子调用原始 `log` 函数 (`ol.apply(this, arguments)`)，原始 log 函数内部对 Protobuf 对象执行了 `String()` 转换，导致对象被显示为 `[instanceof e]`。

**修复：** `log` 钩子不再调用原始 log 函数，直接使用 `objToStr()` 自行序列化。

#### 问题 2：压缩数据包无解码输出

**根因：** 对于压缩数据包（如 `ActivityListP Size:14575`），`parserS2C` 返回的是解压后的中间缓冲区，而非最终解码后的 Protobuf 对象。最终解码数据通过 `log(msg, decodedData)` 传递。

**修复：** `parserS2C` 钩子捕获原始函数返回值存入 `window._aq_s2c_current.decodedResult`，`log` 钩子捕获 `arguments[1]` 存入 `logData`。`parserS2C` 输出时优先使用 `logData`，回退到 `decodedResult`。

#### 问题 3：重复日志输出

**根因：** 多次注入脚本时，旧的钩子函数被新钩子替换后，新钩子内部引用的 `os`/`op` 指向的是旧钩子而非原始函数，形成 `钩子 → 钩子 → 原始函数` 的链式调用，导致每条协议输出两次。

**修复：** 引入 `window._aq_orig` 持久化命名空间，在挂钩前先恢复原始函数，每次注入从持久化引用获取原始函数。

```javascript
window._aq_orig = window._aq_orig || {};

// 挂钩前恢复
var restoreOrig = function(cn) {
  var p = C.prototype;
  if (p._aq_hk_send && window._aq_orig[cn + '_send']) {
    p.send = window._aq_orig[cn + '_send'];
  }
  // ...
};

// 挂钩时保存
var os = window._aq_orig[cn + '_send'] || p.send;
window._aq_orig[cn + '_send'] = os;
```

#### 问题 4：模块名重复显示

**根因：** `getModuleName()` 返回的字符串已包含括号（如 `TaskProxy(17)`），但日志拼接时额外追加了模块 ID 参数，导致输出为 `TaskProxy(17)(17)`。

**修复：** 移除多余的参数拼接，直接使用 `getModuleName()` 的返回值。

#### 问题 5：分段输出与 Hex 冗余

**根因：** `parserS2C` 钩子分多行输出协议头、Hex、消息、数据、解析结果，导致一条协议在控制台占据 5-6 行。

**修复：** 合并为单行输出，去除 Hex 显示，`log` 钩子静默化（仅记录不输出）。

---

## 五、最终脚本 v4.2 完整说明

### 5.1 输出格式

```
C2S 发送：
  🚀 [C2S]SocketMgr.ActivityProxy(18).GetCommonActivityInfoList(22) { "codes": [...] }

S2C 接收：
  📥 [S2C]SocketMgr.ChatProxy(19).NewMessageP(7) { "message": { "0": { "content": "...", "fromPlayer": { "name": "太叔伟泽", ... } } } }
```

### 5.2 全局变量

| 变量 | 类型 | 说明 |
|------|------|------|
| `window._aq_protocol_log` | Array | 所有协议记录的完整日志 |
| `window._aq_objToStr(obj)` | Function | 手动序列化任意对象为 JSON |
| `window._aq_orig` | Object | 持久化存储的原始函数引用 |
| `window._aq_s2c_current` | Object | 当前 S2C 数据包的上下文信息 |

### 5.3 脚本结构

```
(function() {
  // 1. 版本号
  // 2. 格式化输出函数 _aqOut()
  // 3. 对象序列化引擎 objToStr()
  // 4. 原始函数恢复机制 restoreOrig()
  // 5. 钩子挂载函数 hook()
  //    5.1 send 钩子 (C2S)
  //    5.2 parserS2C 钩子 (S2C)
  //    5.3 log 钩子 (静默捕获)
  // 6. 清理旧钩子
  // 7. 挂载到 SocketMgr 和 GameRoomSocket
  // 8. 重试机制（若 Socket 类尚未加载）
})();
```

---

## 六、使用指南

### 6.1 注入方式

1. 打开浏览器控制台 (F12)
2. 复制 [jb.txt](file:///d:/Backup/Downloads/212/jb.txt) 全部内容
3. 粘贴到控制台并回车执行
4. 观察输出：`[阿庆协议抓包] v4.2 就绪 (N 钩子)`

### 6.2 验证方法

```javascript
// 检查钩子状态
window._aq_orig              // 应包含 SocketMgr_send, SocketMgr_s2c 等
window._aq_protocol_log      // 应包含已捕获的协议记录

// 手动序列化任意对象
window._aq_objToStr(someObject)
```

### 6.3 查看完整记录

```javascript
// 控制台查看
console.table(window._aq_protocol_log);

// 导出为 JSON
JSON.stringify(window._aq_protocol_log, null, 2);
```

---

## 七、总结与建议

### 7.1 技术总结

1. **协议类型**：游戏采用自定义二进制协议，基于 Protobuf 序列化，部分大包使用 QuickLZ 压缩
2. **路由体系**：二级路由（模块 ID + 命令 ID），通过 `getModuleName`/`getCmdName` 映射
3. **通信类**：`SocketMgr` 为主通信类，`GameRoomSocket` 处理副本/房间场景，两者共享同一套协议架构
4. **输出限制**：游戏重写了 `console` 方法，所有日志均经过 `h5SB_dalan.js:375` 的包装器，无法绕过

### 7.2 潜在改进方向

1. **WebSocket 层钩子**：直接 Hook `WebSocket.prototype.send` 和 `WebSocket.onmessage`，可在更底层捕获原始二进制帧，不受游戏版本更新影响
2. **自动重连**：页面路由切换时自动重新注入脚本
3. **日志过滤**：支持按模块/命令过滤，只关注感兴趣的协议
4. **数据导出**：一键导出协议日志为 JSON 文件，方便离线分析

### 7.3 注意事项

- 脚本依赖于 Cocos Creator 的 `cc.js.getClassByName()` 方法，若游戏框架升级导致该方法变更，需相应调整
- 若游戏版本更新改变了 `parserS2C` 或 `send` 的函数签名，钩子逻辑需同步更新
- 对于超大压缩数据包（>10000 bytes），日志记录可能不完整，属于正常截断行为

---

## 八、控制台发包指南

### 8.1 send 方法签名

通过逆向分析，`SocketMgr` 的 `send` 方法签名如下：

```javascript
send(moduleId, cmdId, data, args)
// moduleId: 模块ID (数字)
// cmdId: 命令ID (数字)
// data: 协议数据对象 (自动 Protobuf 编码)
// args: 额外参数 (可选)
```

**注意：** `moduleId` 是**数字**而非模块对象，`getModuleName(id)` 负责将数字映射为模块名。

### 8.2 发包辅助脚本

脚本位于 [send_helper.js](file:///d:/Backup/Downloads/212/send_helper.js)，在控制台执行后提供以下全局函数：

| 函数 | 说明 |
|------|------|
| `send(moduleId, cmdId, data)` | 按模块ID/命令ID发包 |
| `sendByName(moduleName, cmdName, data)` | 按模块名/命令名发包 |
| `listModules()` | 列出所有可用模块 |
| `listCommands(moduleId)` | 列出指定模块的所有命令 |
| `inspectProto(moduleId, cmdId)` | 查看指定命令的协议字段定义 |

### 8.3 使用示例

```javascript
// 示例1：发送图鉴星级点数请求
send(24, 5, {handBookType: 1, code: 250005009});

// 示例2：按名称发送（等价于示例1）
sendByName('HandBookProxy', 'GetStarHandBookPoint', {handBookType: 1, code: 250005009});

// 示例3：查看所有模块
listModules();

// 示例4：查看 HandBookProxy(24) 的所有命令
listCommands(24);

// 示例5：查看模块 24 命令 5 的协议字段定义
inspectProto(24, 5);
```

### 8.4 快速参考：常用模块/命令

| 模块ID | 模块名 | 说明 |
|--------|--------|------|
| 1 | LoginProxy | 登录 |
| 2 | LoginGameProxy | 游戏登录（含 Ping） |
| 5 | TestProxy | 测试（GM 指令） |
| 6 | BagProxy | 背包 |
| 7 | MailProxy | 邮件 |
| 17 | TaskProxy | 任务 |
| 18 | ActivityProxy | 活动 |
| 19 | ChatProxy | 聊天 |
| 23 | HeroProxy | 英雄 |
| 24 | HandBookProxy | 图鉴 |
| 25 | FormationProxy | 阵型 |

**TestProxy(5) 常用 GM 命令：**

| 命令ID | 命令名 | 说明 |
|--------|--------|------|
| 1 | addItem | 添加物品 |
| 3 | addHero | 添加英雄 |
| 5 | addMoney | 添加货币 |
| 7 | SendSystemMail | 发送系统邮件 |
| 9 | ModifyPlayerLevel | 修改玩家等级 |
| 11 | SetVipLevel | 设置 VIP 等级 |

### 8.5 工作原理

`send()` 内部自动完成以下操作：

1. 通过 `cc.js.getClassByName('SocketMgr')._ins` 获取单例实例
2. 调用 `getModuleName(moduleId)` / `getCmdName(moduleId, cmdId)` 解析名称
3. 通过 `_responseDic` 验证协议定义是否存在
4. 调用原始 `send(moduleId, cmdId, data)` 方法，内部自动完成 Protobuf 编码和二进制帧组装
5. 发送的数据包会同步被 `jb.txt` 的抓包钩子捕获并输出

**不需要手动编码或序列化数据**——游戏引擎会自动处理 Protobuf 编码。