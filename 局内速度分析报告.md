速度异常检测机制逆向分析报告
一、控制台日志协议交互解读
1.1 错误触发时序链（日志文件 sdk.ygkksy.com-1785792598541.md 第14055-14091行）

Plain Text

5:28:47:774  [S2C] TaskProxy.TaskP(7)  err:0          ← 任务推送正常
5:28:47:775  离开房间失败，错误码10000361              ← 客户端打印错误
             ↳ B @ index.0f8ab.js → e.QuitRoomRResponse
5:28:47:784  [S2C] QuitRoomR(5) { "result":1, "err":10000361 }  ← 服务端拒绝退房
5:28:47:803  H5Sdk cmd { type:'admtReport' }          ← 触发反作弊数据上报
1.2 正常退房对比（日志文件 sdk.ygkksy.com-1785791981935.md 第787行）
正常退房时 QuitRoomR 返回 err:0 并携带 clientReward（装备/奖励列表），错误时仅返回 {result:1, err:10000361} 无奖励数据。

1.3 QuitRoom C2S 上报数据结构（日志第144-199行）

JSON

{
  "battleType": 3,
  "type": 1,                    // 1=胜利 0=失败
  "extra": "{\"result\":1,\"resultReason\":\"EnemyAllDead\",...\"totalHurt\":\"343800\",...\"killNum\":582}",
  "endFrame": 3647,             // ★ 最终帧数（服务端校验核心字段）
  "totalTime": 364,             // ★ = floor(endFrame / TICK_INTERVAL_IN_SEC) = 3647/10
  "operations": [               // ★ 玩家操作记录（含每条操作的 operFrame）
    { "operationType":1, "playerId":254463, "operFrame":0, "args":"{}" },
    { "operationType":3, "playerId":254463, "operFrame":0, "args":"{\"frame\":0}" },
    ...
  ]
}
服务端校验维度：endFrame（游戏帧）、totalTime（游戏时长=endFrame/10）、operations（操作时间线）、extra（伤害/击杀统计）。

二、双层速度检测机制定位
2.1 第一层：客户端速度检测（_checkCheat）
位置：main_index.js#L458022-L458058


JavaScript

// 帧驱动入口 — 由 cocos scheduler update 调用
e.prototype.onUpdate = function (e) {
  this._speedCheckEnable &&
    ((this._updateTime += e),  // 累加 cocos 传入的 delta（受 setTimeScale 影响）
    this._updateTime >= this._nextCheatTickTime &&
      ((this._nextCheatTickTime = this._updateTime + 15), this._checkCheat()));
};

e.prototype._checkCheat = function () {
  var e = this._updateTime,        // 客户端累积时间（加速后会膨胀）
      t = this.serverTime();       // 服务端真实时间 = Date.now()/1000 + timeDvalue
  if (this._lastServerTime <= 0)
    ((this._lastServerTime = t), (this._lastUpdateTime = e), (this._nextCheatJudgeTime = e + 60));
  if (e >= this._nextCheatJudgeTime) {
    // ★ 核心判定：客户端时间流逝 / 服务端时间流逝 > 1.2 则触发
    if ((e - this._lastUpdateTime) / (t - this._lastServerTime) > 1.2) {
      this.stopPingSche();
      // 弹窗文本（Unicode转义）：\u68c0\u67e5\u5230\u901f\u5ea6\u5f02\u5e38\uff0c\u5df2\u65ad\u5f00\u8fde\u63a5\u3002
      // = "检查到速度异常，已断开连接。"
      var o = { noCancelBtn:!0, clickCB:function(e){...exitGame()}, commonTips: i18n(...), clickBtnClose:!0 };
      L.UIMgr.Ins.open(h.CommonTipsDialog, p.UILayerType.View, o);
      a.SocketMgr.Ins.close();      // 主动断开 Socket
      this._speedCheckEnable = !1;
    }
    ((this._lastUpdateTime = e), (this._lastServerTime = t), (this._nextCheatJudgeTime = e + 60));
  }
};
检测开关：main_index.js#L457961


JavaScript

P.SDKMgr.Ins.getPerformConfig(B.PerformType.CheckSpeed, 1) && (this._speedCheckEnable = !0);
// PerformType.CheckSpeed = 40003（line 352421）
判定逻辑：

每 15 秒累积触发一次 _checkCheat

首次记录基准值，60 秒后开始判定

比值 = 客户端累积delta / 服务端真实时间差 > 1.2 → 触发弹窗+断开

5倍速时比值 ≈ 5.0，远超 1.2 阈值

AceHacker V9.2 绕过方式（Step 1）：


JavaScript

SDKMgr.Ins.performConfig[40003] = false;  // 关闭客户端检测开关
此绕过有效，客户端弹窗"检查到速度异常，已断开连接"不会出现。

2.2 第二层：服务端速度检测（错误码 10000361）
位置：main_index.js#L794229-L794270（QuitRoomR 响应回调）


JavaScript

var B = function (e) {  // QuitRoomR 响应回调
  (i.quitSuccess = !0), _.TimeMgr.Ins.remove(d);
  if (0 == e.err) {
    // 正常流程：发放奖励
    ...
  } else {
    // ★ err = 10000361 走此分支
    console.warn("\u79bb\u5f00\u623f\u95f4\u5931\u8d25\uff0c\u9519\u8bef\u7801" + e.err);
    // = "离开房间失败，错误码10000361"
    (m.result = f.EBattleResult_Battle_Enum.EBattleResultLose),  // 结果强制改为败
    (m.uploadExt = null),
    p.PopMgr.Ins.addMidMsg(a.ErrorCodeLO.get(e.err).errorStr);
    // ↑ 从 ErrorCodeLO 配置表读取错误描述文本并弹出
    // errorStr 即用户看到的 "检测当前速度异常，请检查当前设备"
  }
  (l(), n(!0));
};
ErrorCodeLO 数据源：main_index.js#L442827-L442866

错误码表从二进制配置文件运行时加载（BinaryReader.readBytes）

10000361 的 errorStr 来自配置，非源码硬编码

用户看到的弹窗文本"检测当前速度异常，请检查当前设备"= 服务端通过配置下发的错误描述

服务端检测维度（推断）： 服务端在收到 QuitRoom 请求时，校验以下数据的内部一致性：

totalTime（= endFrame/10，游戏时长）vs 服务器记录的真实经过时间

operations 中各操作的 operFrame 分布是否合理

extra 中伤害/击杀数据与帧数的比率是否异常

若 totalTime / 真实时间 > 阈值 → 返回 err=10000361

三、帧同步系统深度分析
3.1 三种帧同步模式
选择逻辑：main_index.js#L269745-L269752


JavaScript

e.moduleConfig.syncModel == H.EFrameSyncType.Tcp     → FrameSyncTCP    （服务端权威）
e.moduleConfig.syncModel == H.EFrameSyncType.Local    → FrameSyncCocos  （客户端权威）★
e.moduleConfig.syncModel == H.EFrameSyncType.AsynTcp  → FrameSyncAsynTCP（异步服务端权威）
挂机/主PVE战斗类型使用 Local 同步：main_index.js#L276933-L276934


JavaScript

e.prototype.getFrameSyncType = function () { return i.Local; };
3.2 FrameSyncCocos — 客户端权威帧推进（关键）
位置：main_index.js#L450918-L450927


JavaScript

t.prototype.update = function (e) {  // e = cocos scheduler delta（受 setTimeScale 影响）
  if (this._delegate && !this._isPause) {
    var t = Math.min(1e3 * e, this._oneCocosFrameTime);
    t = Math.min(t * this._timeScale, this._oneFrameTime);  // delta × timeScale
    this._frameResidual -= t;
    if (this._frameResidual <= 0) {
      this.renderTick(t / 1e3, 0);
      this.logicTick();                              // ★ 自由递增 _tick，无服务端上限
      this._frameResidual += this._oneFrameTime;
    } else {
      this.renderTick(t / 1e3, this._frameResidual);
    }
  }
};
FrameSyncBase.logicTick：main_index.js#L450819-L450821


JavaScript

t.prototype.logicTick = function () {
  this._delegate.logicTick(++this._tick);  // ★ _tick 无上限自由递增
};
结论：Local 同步模式下，setTimeScale(5) → cocos delta 放大 5 倍 → _tick 以 5 倍速推进 → endFrame 和 totalTime 均为真实时间的 5 倍。

3.3 FrameSyncTCP — 服务端权威帧推进（对比）
位置：main_index.js#L451055-L451097


JavaScript

t.prototype.update = function (e) {
  ...
  var t = this._expected_tick - this._tick;  // ★ 服务端预期帧 - 客户端当前帧
  if (t > this._safeFrameGap) {
    // 落后服务端 → 动态加速追赶（最大 _maxTimeScale=3）
    this._setTimeScale(Math.min(this._baseTimeScale + (t - this._safeFrameGap) * this._chaseSpeedStep, this._maxTimeScale));
  } else if (t < -2) {
    // 超前服务端 → 减速
    this._setTimeScale(Math.max(this._baseTimeScale - 0.1 * Math.abs(t), 0.5));
  } else {
    this._setTimeScale(this._baseTimeScale);  // 正常同步
  }
  ...
  if (this._tick <= this._expected_tick) {
    this.logicTick();  // ★ 仅当未超过服务端帧时才推进
  } else {
    this.logWarning("当前帧已逼近预期帧，等待服务器时钟同步");  // ★ 超限时等待
  }
};
结论：TCP 同步模式下，_tick 被 _expected_tick（服务端下发）硬限制，setTimeScale hack 无法使帧超过服务端节奏。但 Local 模式无此限制。

3.4 TICK_INTERVAL_IN_SEC 常量
位置：main_index.js#L260412


JavaScript

o.TICK_INTERVAL_IN_SEC = 10;  // 10帧/秒，每帧 = 0.1秒
退房上报换算：totalTime = Math.floor(endFrame / 10) → 帧数/10 = 游戏秒数

四、自动托管系统（HostingMgr）分析
4.1 托管管理器架构
位置：main_index.js#L595231-L595409


JavaScript

// 托管定时器注册
e.prototype.bindDelegateServer = function (e) {
  var o = 1;  // 默认1秒间隔
  P.BattleRoomHelper.Ins.getProvider(e.battleType).getFrameSyncType() != P.EFrameSyncType.Tcp && (o = 0.5);
  // ★ Local 模式下间隔 0.5 秒
  this.timerHandler = h.TimeMgr.Ins.callInterval(o, this.update.bind(this));
};
4.2 TimeMgr.callInterval 受 setTimeScale 影响
位置：main_index.js#L851275-L851296


JavaScript

e.prototype.onUpdate = function (e) {  // e = cocos scheduler delta
  this._timestamp += e;  // ★ 累积原始 delta（加速后膨胀）
  var t = r.UpdateMgr.Ins.timeScale;
  this._realTime += t > 0 ? e / t : 0;  // realTime 做了补偿（除以 timeScale）
  // 但定时器触发基于 _timestamp（未补偿）→ 定时器会随加速提前触发
  while (this._timers.length > 0 && this._timestamp >= this._timers[0].nextTrick) {
    ...
    o.doAction();  // ★ 定时器回调被加速触发
  }
};
结论：setTimeScale(5) 后，HostingMgr 的 0.5 秒定时器实际以 0.1 秒真实间隔触发，托管 AI 执行频率提高 5 倍。但每次执行检查的是 battleEngine.current_tick（同样 5 倍速），因此操作在游戏时间维度上是正确的。

4.3 托管操作记录
位置：main_index.js#L793948


JavaScript

i.operFrame = S.TDServer.Ins.curFrame;  // 操作标记当前帧号
所有操作（手动/托管）均以当前 curFrame（=_tick）为时间戳。5 倍速下帧号 5 倍膨胀，但操作间帧间距在游戏时间维度保持正确。

五、AceHacker V9.2 变速脚本机制对照
5.1 脚本策略分解
Step	劫持目标	作用	效果
1	SDKMgr.performConfig[40003]=false	关闭客户端 _checkCheat	✓ 客户端弹窗不再出现
2	scheduler.setTimeScale	cocos 全局调度器加速	渲染+所有update循环5倍速
3a	BattleFrame._setTimeSpeed	劫持倍速按钮 → 强制5x	battleMain.setTimeScale(5)
3d	BattleFrame.playBattlePreEnd	拦截提前结束	防止引擎FINISHED导致提前退房
4	EngineStateChangedParser.afterTick	拦截FINISHED状态(state=3)	阻止引擎状态机进入结束态
5	BattleFrame.onWillFinish	备用拦截FSM退出	第三道防线
5.2 失败根因
AceHacker V9.2 成功绕过了客户端检测，但未能绕过服务端检测：

Local 同步模式下，setTimeScale(5) → _tick 5 倍速推进 → endFrame 膨胀

战斗结束时 QuitRoom 上报 endFrame/totalTime 为真实游戏时长（正确），但真实经过时间仅为 1/5

服务端比对 totalTime（游戏时长）vs 服务器记录的真实经过时间 → 比值 ≈ 5 → 返回 err=10000361

AceHacker 拦截了 playBattlePreEnd（防止提前结束），但当所有敌人死亡时引擎正常 FINISHED → 仍然触发退房 → 服务端拒绝

5.3 正常速度倍率
位置：main_index.js#L255437


JavaScript

o.BATTLE_SPEED_TIME_SCALE = [1, 1.6];  // 官方仅支持 1x 和 1.6x
5 倍速远超官方 1.6x 上限，服务端校验阈值必然覆盖此范围。

六、用户测试场景分析
用户观察："5x + 不动10秒 + 自动上床 + 全局托管 + 正常伤害 = 无检测"
根因推断：

_checkCheat 已被 AceHacker Step 1 禁用 → 客户端层不触发

服务端检测存在容差阈值或基于多维度综合判定，而非单纯时间比：

当托管 AI + 正常伤害时，operations 的帧间分布在游戏时间维度完全合法
extra 中伤害/击杀数据与帧数比率正常（无伤害修改）
服务端可能仅对操作模式异常或伤害异常的战斗进行严格审查
纯加速但游戏逻辑数据一致的战斗可能未触发服务端告警阈值
"不动10秒"的作用：让初始帧推进稳定，避免开局瞬间的操作突发（如立即放床/召唤）导致操作帧分布异常

对比：手动5x操作时触发检测的原因：

手动操作在 5x 下可能在极短真实时间内连续提交多个操作
operFrame 虽然不同（帧在推进），但服务端可能检测到真实提交时间间隔过短
或手动操作导致战斗异常快速结束（敌人过早死亡），totalHurt/totalTime 比率异常
七、绕过方案建议
方案 A：时间补偿（推荐 — 最小改动）
核心思路：在 QuitRoom 上报时，将 endFrame 和 operations.operFrame 按倍率缩放回 1x 对应值，使 totalTime 匹配真实经过时间。

实现要点：


JavaScript

// 劫持 TDServerProxy.quitRoomCommit，在上报前缩放 endFrame
var TDServerProxy = cc.js.getClassByName('TDServerProxy');
var _origQuitRoomCommit = TDServerProxy.prototype.quitRoomCommit;
TDServerProxy.prototype.quitRoomCommit = function(uuid, result, endFrame, cb) {
  if (window._aq_speed && window._aq_speed.enabled) {
    var scale = window._aq_speed.target;
    // 将 endFrame 缩放回 1x 对应帧数
    endFrame = Math.floor(endFrame / scale);
    // 同时缩放 _clientOperationRecord 中每条操作的 operFrame
    var records = this._clientOperationRecord;
    if (records && records.length > 0) {
      records.getValues().forEach(function(op) {
        if (op.operFrame) op.operFrame = Math.floor(op.operFrame / scale);
      });
    }
    console.warn('[AceHacker] endFrame 缩放: ' + endFrame + ' (1/' + scale + ')');
  }
  return _origQuitRoomCommit.call(this, uuid, result, endFrame, cb);
};
风险：extra 中的 totalHurt 等数据若与帧数有隐含关联，缩放后可能不一致。需同时缩放 extra 中的时间相关字段。

方案 B：帧计数器限速（推荐 — 根本性解决）
核心思路：不使用 setTimeScale 加速帧推进，改为仅加速渲染层，保持 _tick 按真实时间推进。

实现要点：


JavaScript

// 劫持 FrameSyncCocos.update，分离渲染速度和逻辑帧速度
var FrameSyncCocos = cc.js.getClassByName('FrameSyncCocos');
var _origUpdate = FrameSyncCocos.prototype.update;
FrameSyncCocos.prototype.update = function(dt) {
  if (window._aq_speed && window._aq_speed.enabled) {
    // 渲染层加速：renderTick 用加速后的 dt
    // 逻辑层限速：logicTick 仍按原始 dt 推进 _frameResidual
    var speed = window._aq_speed.target;
    var renderDt = dt * speed;  // 渲染加速
    var logicDt = dt;            // 逻辑保持1x
    // 自定义 update 逻辑：用 logicDt 推进 _frameResidual，用 renderDt 推进 renderTick
    if (this._delegate && !this._isPause) {
      var t = Math.min(1e3 * logicDt, this._oneCocosFrameTime);
      t = Math.min(t * this._timeScale, this._oneFrameTime);
      this._frameResidual -= t;
      if (this._frameResidual <= 0) {
        this.renderTick(renderDt / 1e3, 0);
        this.logicTick();
        this._frameResidual += this._oneFrameTime;
      } else {
        this.renderTick(renderDt / 1e3, this._frameResidual);
      }
    }
    return;
  }
  return _origUpdate.call(this, dt);
};
效果：_tick 按真实时间推进，endFrame/totalTime 与真实经过时间一致，服务端无法检测。渲染层仍以 5x 速度播放动画。

限制：战斗逻辑（出兵、倒计时）按 1x 推进，实际战斗时长不变，仅动画播放更快。

方案 C：服务端时间伪造（高风险）
核心思路：劫持 serverTime() 返回值，使 _checkCheat 的比值计算恒 ≤ 1.2。


JavaScript

// 仅在 _checkCheat 未被关闭时的补充方案
var gameFrame = cc.js.getClassByName('GameFrame'); // 或对应类名
var _origServerTime = gameFrame.prototype.serverTime;
gameFrame.prototype.serverTime = function(e) {
  if (window._aq_speed && window._aq_speed.enabled) {
    // 伪造服务端时间，使其与客户端加速同步
    return _origServerTime.call(this, e) * window._aq_speed.target;
  }
  return _origServerTime.call(this, e);
};
风险：serverTime 全局使用（Ping同步、跨天检测等），伪造会影响其他系统。

方案 D：操作间隔分散（配合方案A）
核心思路：在 commitClientRequest 中添加真实时间戳，上报时将操作按真实时间重排，使 operFrame 分布看起来像 1x。


JavaScript

// 在 createOperationReq 中记录真实时间戳
var _origCreate = TDServerProxy.prototype.createOperationReq;
TDServerProxy.prototype.createOperationReq = function(playerId, type, args) {
  var op = _origCreate.call(this, playerId, type, args);
  op._realTimestamp = Date.now();  // 附加真实时间
  return op;
};
// 退房时按真实时间重算 operFrame
// operFrame = (realTimestamp - battleStartTime) / 100 (100ms per frame at 10fps)
八、结论
检测层	机制	触发条件	当前状态
客户端 _checkCheat	_updateTime/serverTime 比值 > 1.2	5x 加速后 60 秒触发	✓ AceHacker 已绕过
服务端 10000361	totalTime vs 真实经过时间 + 多维度一致性校验	QuitRoom 时 endFrame/totalTime 与真实时间不匹配	✗ 当前未绕过
根因：Local 帧同步（FrameSyncCocos）下 setTimeScale 直接放大 _tick 递增速率，导致 endFrame/totalTime 膨胀为真实时间倍数，服务端检测到时间比率异常。

推荐优先级：方案 B（帧计数器限速）> 方案 A（时间补偿）> 方案 D（操作分散）> 方案 C（时间伪造）

方案 B 从源头解决问题，不修改上报数据，最不容易被服务端新增检测规则发现。方案 A 需精确缩放所有时间相关字段，有一定不一致风险。